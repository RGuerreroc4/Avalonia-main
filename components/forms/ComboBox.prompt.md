Dropdown for 4+ exclusive options; below that use RadioButtons. The ControlCatalog settings drawer stacks several at `HorizontalAlignment="Stretch"`.

```jsx
<ComboBox items={['Fluent','Simple']} value={theme} onChange={setTheme} />
```
