For settings that take effect immediately — no Apply button. Use a CheckBox instead when the change is staged in a form.

```jsx
<ToggleSwitch checked={on} onChange={setOn} onContent="Enabled" offContent="Disabled" />
```
