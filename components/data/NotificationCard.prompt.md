Transient notification. Stack them bottom-right; keep the body to one or two lines.

```jsx
<NotificationCard type="success" title="Build succeeded" onClose={dismiss}>
  ControlCatalog.Desktop compiled in 4.2s.
</NotificationCard>
```
