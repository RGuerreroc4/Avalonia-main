Row primitive for the catalog's nav drawer and any ListBox. Selection is an accent tint, never a left border.

```jsx
<ListItem selected icon={<PathIcon data={ICONS.Layers} size={16} />}>Layout</ListItem>
```
